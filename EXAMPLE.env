
# Example environment variables for Labcamp-Fine-Tuning-SLM
# Copy this file to `.env` or set the variables in your CI / secrets manager.

# AWS / SageMaker
AWS_REGION=us-east-1
SAGEMAKER_ROLE_ARN=arn:aws:iam::<ACCOUNT-ID>:role/<ROLE-NAME>
INSTANCE_TYPE=ml.g6.xlarge
VOLUME_SIZE=150

# ECR image (account specific) - e.g. unsloth/unsloth or <account>.dkr.ecr.<region>.amazonaws.com/repo
ECR_IMAGE_NAME=docker-image-name

# Optional tokens (do NOT commit real tokens)
HUGGINGFACE_HUB_TOKEN=
WANDB_API_KEY=

# Output buckets / prefixes (optional)
S3_OUTPUT_BUCKET=labcamp-1-finetuning

# Local dev overrides
# SOURCE_DIR=./training_file

